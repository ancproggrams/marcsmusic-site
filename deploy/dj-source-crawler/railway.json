{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "cronSchedule": "*/30 * * * *",
    "restartPolicyType": "NEVER",
    "startCommand": "python /app/crawler.py"
  }
}
