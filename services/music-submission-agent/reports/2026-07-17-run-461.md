# Music Submission Agent — Run 461

Date: 2026-07-17

## Retry coverage

Run 461 retried the four routes that remained browser errors in Run 460, using a 20-second browser timeout. All four remained errors: Wepa.Fm and WRIR ended with an agent-browser process error; Songtradr and Monstercat returned `ERR_HTTP_RESPONSE_CODE_FAILURE`. No route was counted as a successful inspection. The committed inventory remains fully represented by browser-result records: 645 of 645 URLs, with four routes explicitly blocked/error.

Retry evidence is stored under `data/portal-browser-tests/run-461-retry-*`; the aggregate is [run461-portal-retry-summary.json](../data/run461-portal-retry-summary.json).

## Submission handling

No active form submit, upload, login, CAPTCHA, consent, payment or email action was performed. The four routes remain `needs_manual_review`/error candidates for human follow-up.

## Validation

- Agent-browser retries completed with inspect-only mode and a 20-second timeout.
- `DATABASE_PATH=./data/submissions.sqlite EXPORT_DIR=./data/exports npm run run:once` completed and drained 20 verification jobs.
- `npm test` passed: 19/19 tests.
- `npm run build` passed.
- `npm run lint` was not rerun because the previous run's lint process produced no output within the observation window; no lint-green claim is made.
- No fields were filled, buttons clicked, files uploaded, forms submitted, logins attempted, CAPTCHAs solved, consents accepted or payments initiated.
