# Music Submission Agent — Run 431

Date: 2026-07-17

## Progress toward full portal coverage

The repository inventory contains 644 unique HTTP(S) portal candidates. This run passively inspected 32 unique routes with agent-browser: 30 were inspected successfully and 2 ended as errors/unreachable. The complete summary is in [run431-portal-coverage-summary.json](../data/run431-portal-coverage-summary.json), with raw shard evidence under `data/portal-browser-tests/`.

Observed across the 32 routes:

- 15 form surfaces;
- 16 login boundaries;
- 1 payment boundary;
- 0 CAPTCHA/human-check surfaces;
- 0 auto-submit candidates.

The routes with login or payment boundaries remain manual review. No protected flow was opened beyond passive navigation.

## Track-fit routing

The owner-supplied routing manifest from Run 430 remains authoritative:

- Carnival → Latin/World and eclectic radio routes;
- Strijd → Dutch Pop/Pop and Dutch/Benelux routes;
- Geen Afscheid - Radio Edit → Pop, Reggae/Dub/World and eclectic radio;
- Curaçao - Radio Edit → Afrobeat/Afropop, Pop and World/Latin;
- Summer Time and Weekend Mode → House/Electronic-only portals.

No outreach was sent in this run. Track selection remains manual until rights, metadata, radio-edit status, eligibility and portal-specific requirements are confirmed.

## Runtime and validation

- `DATABASE_PATH=./data/submissions.sqlite EXPORT_DIR=./data/exports npm run run:once` completed and drained 20 verification jobs.
- `npm run build` passed.
- `npm test` passed: 19 tests.
- Targeted TypeScript, Prettier and `git diff --check` passed.
- The full `npm run lint` chain was time-limited at 30 seconds without error output; targeted checks passed.

## Continuation

Full coverage is not complete: 612 candidates remain outside this run's verified coverage count. The next run must continue with new browser shards and deduplicate against the URLs recorded here.
